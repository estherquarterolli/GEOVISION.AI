# GeoVision.AI — Serviço de classificação de risco
#
# O artefato atual foi exportado em TensorFlow 2.20. O runtime local usa
# Python 3.11, compatível com essa versão no Windows. A inferência é local.

# --- API ---------------------------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.3
pydantic==2.13.4
pydantic-settings==2.15.0
python-multipart==0.0.32      # upload de imagem via multipart/form-data

# --- Autenticação ------------------------------------------------------------
# Faixa em vez de versão exata: estes dois entraram junto com o deploy e não
# foram travados contra um ambiente já testado como o resto do arquivo.
# bcrypt publica wheel abi3, então funciona em qualquer CPython >= 3.9.
PyJWT>=2.10,<3
bcrypt>=4.2,<6
email-validator>=2.2,<3

# --- Imagem ------------------------------------------------------------------
pillow==12.3.0
# Mantido em 2.2 para compatibilidade entre TensorFlow e OpenCV no Python 3.11.
numpy==2.2.6

# --- Inferência --------------------------------------------------------------
# Modelo MobileNetV2 visual exportado em Keras.
tensorflow==2.20.0

# --- Integrações -------------------------------------------------------------
httpx==0.28.1                 # chamadas ao Supabase, Resend e WhatsApp Cloud API
supabase==2.31.0

# --- Testes ------------------------------------------------------------------
pytest==9.1.1
pytest-asyncio==1.4.0
